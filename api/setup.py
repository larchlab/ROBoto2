#!/usr/bin/python
import setuptools

setuptools.setup(
    name='roboto2',
    version='1.0',
    packages=setuptools.find_packages(),
    install_requires=[
    ],
    tests_require=[
    ],
    zip_safe=False,
    entry_points='',
)